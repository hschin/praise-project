---
phase: 01-auth-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/fixtures/users.yml
  - test/controllers/registrations_controller_test.rb
  - test/controllers/passwords_controller_test.rb
  - test/controllers/decks_controller_test.rb
  - test/controllers/songs_controller_test.rb
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03

must_haves:
  truths:
    - "rails test test/controllers/ runs and all tests pass (no errors, no failures)"
    - "Test for unauthenticated sign-up page access exists and passes"
    - "Test for valid sign-up flow exists and passes"
    - "Test for unauthenticated /decks redirect exists and passes"
    - "Test for unauthenticated /songs redirect exists and passes"
    - "Test for password reset page access exists and passes"
  artifacts:
    - path: "test/fixtures/users.yml"
      provides: "User fixture with hashed password for Devise sign_in"
      contains: "Devise::Encryptor.digest"
    - path: "test/controllers/registrations_controller_test.rb"
      provides: "AUTH-01 signup flow tests"
      exports: ["test 'GET sign up page returns 200'", "test 'valid sign up redirects to decks'", "test 'invalid sign up re-renders form'"]
    - path: "test/controllers/passwords_controller_test.rb"
      provides: "AUTH-03 password reset request tests"
      exports: ["test 'GET password reset page returns 200'", "test 'POST password reset with valid email enqueues reset'"]
    - path: "test/controllers/decks_controller_test.rb"
      provides: "AUTH-02 session persistence tests (fixed)"
      contains: "include Devise::Test::IntegrationHelpers"
    - path: "test/controllers/songs_controller_test.rb"
      provides: "AUTH-02 songs protection tests (fixed)"
      contains: "include Devise::Test::IntegrationHelpers"
  key_links:
    - from: "test/controllers/registrations_controller_test.rb"
      to: "devise_for :users routes"
      via: "new_user_registration_path, user_registration_path"
      pattern: "new_user_registration_path"
    - from: "test/controllers/decks_controller_test.rb"
      to: "test/fixtures/users.yml"
      via: "users(:one) in sign_in call"
      pattern: "sign_in users\\(:one\\)"
---

<objective>
Create the Wave 0 test scaffold for Phase 1. All auth integration tests must exist and pass before implementation tasks run — this is the RED phase that makes subsequent GREEN verification possible.

Purpose: The Nyquist sampling rule requires automated tests for every requirement. Two test files are missing entirely (registrations, passwords). Two existing test files are broken (no Devise helpers, wrong route helpers). The user fixture has no encrypted password so sign_in fails silently.

Output: Five test files in correct state, all tests green, `rails test test/controllers/` passes cleanly.
</objective>

<execution_context>
@/Users/hschin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/hschin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-auth-foundation/01-CONTEXT.md
@.planning/phases/01-auth-foundation/01-RESEARCH.md

<interfaces>
<!-- Key patterns from existing codebase for test authoring -->

From test/controllers/decks_controller_test.rb (current broken state — needs these patterns added):
```ruby
# Pattern to ADD — Devise test integration
include Devise::Test::IntegrationHelpers

setup do
  @user = users(:one)
  sign_in @user
end
```

From RESEARCH.md — Devise fixture pattern:
```yaml
# test/fixtures/users.yml
one:
  email: user@example.com
  encrypted_password: <%= Devise::Encryptor.digest(User, 'password123') %>
```

From RESEARCH.md — Route helpers to use (not raw strings):
```ruby
new_user_registration_path    # GET  /users/sign_up
user_registration_path        # POST /users
new_user_session_path         # GET  /users/sign_in
new_user_password_path        # GET  /users/password/new
user_password_path            # POST /users/password
decks_url                     # GET  /decks
songs_url                     # GET  /songs
```

From config/routes.rb — already has:
```ruby
devise_for :users
root "decks#index"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix user fixture and repair existing test files</name>
  <files>test/fixtures/users.yml, test/controllers/decks_controller_test.rb, test/controllers/songs_controller_test.rb</files>
  <behavior>
    - decks_controller_test: unauthenticated GET /decks redirects to new_user_session_path
    - decks_controller_test: authenticated GET /decks returns 200
    - songs_controller_test: unauthenticated GET /songs redirects to new_user_session_path
    - songs_controller_test: authenticated GET /songs returns 200
  </behavior>
  <action>
    1. Update test/fixtures/users.yml: add encrypted_password using ERB — `encrypted_password: <%= Devise::Encryptor.digest(User, 'password123') %>`. The current fixture is empty `{}` which causes Devise sign_in to fail silently.

    2. Rewrite test/controllers/decks_controller_test.rb to include Devise::Test::IntegrationHelpers, set up @user = users(:one) with sign_in in setup block. Fix any broken route helpers (use decks_url, not hardcoded "/decks"). Add tests: (a) unauthenticated GET decks_url redirects to new_user_session_path, (b) authenticated GET decks_url returns 200. Remove or update any existing tests that use wrong patterns.

    3. Rewrite test/controllers/songs_controller_test.rb the same way: include Devise::Test::IntegrationHelpers, sign_in @user in setup. Add tests: (a) unauthenticated GET songs_url redirects to new_user_session_path, (b) authenticated GET songs_url returns 200.

    Run `rails test test/controllers/decks_controller_test.rb test/controllers/songs_controller_test.rb` after each file to confirm green.
  </action>
  <verify>
    <automated>cd /Users/hschin/Dev/praise-project && rails test test/controllers/decks_controller_test.rb test/controllers/songs_controller_test.rb</automated>
  </verify>
  <done>Both test files pass. Unauthenticated redirect and authenticated success tests exist for /decks and /songs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create missing registrations and passwords test files</name>
  <files>test/controllers/registrations_controller_test.rb, test/controllers/passwords_controller_test.rb</files>
  <behavior>
    - registrations: GET new_user_registration_path returns 200
    - registrations: POST user_registration_path with valid params (unique email + password >= 6 chars) redirects to decks_path (root)
    - registrations: POST user_registration_path with invalid params (missing password) returns 422 and re-renders form
    - passwords: GET new_user_password_path returns 200
    - passwords: POST user_password_path with registered email returns 302 (redirect) and enqueues password reset
    - passwords: POST user_password_path with unregistered email returns 422 or still renders form gracefully
  </behavior>
  <action>
    Create test/controllers/registrations_controller_test.rb as ActionDispatch::IntegrationTest. Include Devise::Test::IntegrationHelpers. Use ActionMailer::Base.deliveries to confirm emails enqueued (call ActionMailer::Base.deliveries.clear in setup, set config.action_mailer.delivery_method = :test in test env if not already set). Tests:
    - GET new_user_registration_path → assert_response :success
    - POST user_registration_path with {user: {email: 'new@example.com', password: 'password123', password_confirmation: 'password123'}} → assert_redirected_to root_path (or decks_path) — Devise redirects after sign-up to after_sign_in_path_for which is decks_path
    - POST user_registration_path with {user: {email: '', password: ''}} → assert_response :unprocessable_entity

    Create test/controllers/passwords_controller_test.rb as ActionDispatch::IntegrationTest. Include Devise::Test::IntegrationHelpers. Tests:
    - GET new_user_password_path → assert_response :success
    - POST user_password_path with {user: {email: users(:one).email}} → assert_response :redirect (Devise redirects on success)
    - POST user_password_path with {user: {email: 'nobody@example.com'}} → assert_response :unprocessable_entity

    Note: For the POST passwords test, ActionMailer::Base.deliveries.clear in setup and assert deliveries.count == 1 after the valid POST to confirm reset email is sent.
  </action>
  <verify>
    <automated>cd /Users/hschin/Dev/praise-project && rails test test/controllers/registrations_controller_test.rb test/controllers/passwords_controller_test.rb</automated>
  </verify>
  <done>Both new test files pass. AUTH-01 signup flow (3 cases) and AUTH-03 password reset (2 cases) are covered with automated integration tests.</done>
</task>

</tasks>

<verification>
Run full controller test suite after both tasks complete:

```
cd /Users/hschin/Dev/praise-project && rails test test/controllers/
```

All 5 test files pass. No errors. No failures.
</verification>

<success_criteria>
- `rails test test/controllers/` exits 0 with all tests passing
- test/fixtures/users.yml contains Devise::Encryptor.digest for encrypted_password
- 4 test behaviors for AUTH-02 (decks + songs, authenticated + unauthenticated)
- 3 test behaviors for AUTH-01 (sign-up page, valid sign-up, invalid sign-up)
- 2 test behaviors for AUTH-03 (reset page, valid reset email)
- All tests use Devise route helpers (no hardcoded URL strings)
</success_criteria>

<output>
After completion, create `.planning/phases/01-auth-foundation/01-01-SUMMARY.md` following the summary template.
</output>

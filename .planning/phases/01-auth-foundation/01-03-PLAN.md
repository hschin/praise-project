---
phase: 01-auth-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified:
  - config/environments/development.rb
  - Procfile.dev
  - app/jobs/smoke_test_job.rb
  - app/views/decks/index.html.erb
  - app/views/songs/index.html.erb
autonomous: false
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03

must_haves:
  truths:
    - "Solid Queue worker processes jobs in development (confirmed via log output, not just boot)"
    - "Decks index empty state shows 'No decks yet. Create your first deck.' with a New Deck button"
    - "Songs index empty state shows 'No songs yet. Search for a song to get started.' with a search bar"
    - "Full test suite passes (rails test)"
  artifacts:
    - path: "config/environments/development.rb"
      provides: "Solid Queue adapter configured for development"
      contains: "queue_adapter = :solid_queue"
    - path: "Procfile.dev"
      provides: "jobs process entry for bin/jobs"
      contains: "jobs: bin/jobs"
    - path: "app/jobs/smoke_test_job.rb"
      provides: "Minimal job for verifying worker execution"
      contains: "SmokeTestJob"
  key_links:
    - from: "config/environments/development.rb"
      to: "Solid Queue gem"
      via: "config.active_job.queue_adapter = :solid_queue"
      pattern: "queue_adapter.*solid_queue"
    - from: "Procfile.dev"
      to: "bin/jobs"
      via: "foreman process entry"
      pattern: "jobs: bin/jobs"
---

<objective>
Wire Solid Queue for development and fix empty state copy. This plan completes Phase 1 by confirming the background job infrastructure actually runs (not just boots) and ensuring the Decks and Songs index pages show the correct empty state text per spec.

Purpose: The Phase 1 success criterion requires "Solid Queue background worker runs in both development and production (confirmed via job execution, not just boot)." Currently development.rb has no queue_adapter configured (defaults to :async) and Procfile.dev has no jobs process. Empty state text on both index pages may not match the locked spec.

Output: Solid Queue wired for development, SmokeTestJob created, empty states match spec. Human checkpoint confirms job actually executes. Full test suite green.
</objective>

<execution_context>
@/Users/hschin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/hschin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-auth-foundation/01-CONTEXT.md
@.planning/phases/01-auth-foundation/01-RESEARCH.md

<interfaces>
<!-- Patterns from RESEARCH.md -->

Solid Queue config:
```ruby
# config/environments/development.rb — add inside Rails.application.configure block
config.active_job.queue_adapter = :solid_queue
```

Procfile.dev — current entries (web, css) plus new jobs entry:
```
web: bin/rails server
css: bin/rails tailwindcss:watch
jobs: bin/jobs
```

SmokeTestJob:
```ruby
# app/jobs/smoke_test_job.rb
class SmokeTestJob < ApplicationJob
  queue_as :default

  def perform
    Rails.logger.info "[SmokeTestJob] Worker is alive at #{Time.current}"
  end
end
```

Empty state spec (LOCKED from CONTEXT.md):
- Decks index (no decks): "No decks yet. Create your first deck." + prominent "New Deck" button
- Songs index (no songs): "No songs yet. Search for a song to get started." + search bar
- Simple message + action — no guided onboarding steps
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire Solid Queue for development and fix empty states</name>
  <files>config/environments/development.rb, Procfile.dev, app/jobs/smoke_test_job.rb, app/views/decks/index.html.erb, app/views/songs/index.html.erb</files>
  <action>
    1. In config/environments/development.rb: Add `config.active_job.queue_adapter = :solid_queue` inside the Rails.application.configure block. Without this, Rails uses :async and jobs never hit Solid Queue's database tables.

    2. In Procfile.dev: Add `jobs: bin/jobs` as a new line. Keep existing web and css entries. This makes `bin/dev` (foreman) start the Solid Queue worker alongside the Rails server and CSS watcher.

    3. Create app/jobs/smoke_test_job.rb with the SmokeTestJob class shown in the interfaces section. This is the job used to verify worker execution in the human checkpoint.

    4. Read app/views/decks/index.html.erb and find the empty state block (the section rendered when @decks is empty). Update the text to exactly: "No decks yet. Create your first deck." — with a visible "New Deck" button linking to new_deck_path. Use the established button class: `"bg-indigo-600 text-white text-sm px-4 py-2 rounded hover:bg-indigo-700"`.

    5. Read app/views/songs/index.html.erb and find the empty state block. Update text to exactly: "No songs yet. Search for a song to get started." — with a search input (type="search" or a search form). The search bar can be a simple form that will be wired in Phase 2; for now it should exist visually.

    Run `rails test` after all changes to confirm full suite is green.
  </action>
  <verify>
    <automated>cd /Users/hschin/Dev/praise-project && rails test</automated>
  </verify>
  <done>development.rb has solid_queue adapter. Procfile.dev has jobs entry. SmokeTestJob exists. Both empty state views match spec text. Full test suite passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification — confirm Solid Queue executes jobs and UI matches spec</name>
  <action>Automated setup is complete. Human verifies that: (1) bin/dev starts three processes, (2) SmokeTestJob actually executes via the jobs process, (3) nav bar and auth pages match the locked spec visually.</action>
  <what-built>Solid Queue wired for development (config + Procfile.dev). SmokeTestJob created. Empty states updated on both index pages.</what-built>
  <how-to-verify>
    1. Start the full dev stack: `cd /Users/hschin/Dev/praise-project && bin/dev`
       Confirm three processes start in the terminal output: web, css, jobs.

    2. In a separate terminal, open a Rails console: `rails console`
       Run: `SmokeTestJob.perform_later`

    3. Watch the `jobs` process output in the bin/dev terminal. Within a few seconds you should see:
       `[SmokeTestJob] Worker is alive at [timestamp]`

    4. Confirm the job executed (not just enqueued) — check Solid Queue tables:
       `SolidQueue::Job.last` should show status "finished" (not "queued")

    5. Visit http://localhost:3000/users/sign_in — confirm no nav bar visible, "Praise Project" heading above form.

    6. Sign in or create an account. Visit http://localhost:3000/decks — confirm nav bar shows "Praise Project" on left, Decks | Songs | [your email] | Logout on right.

    7. If you have no decks, confirm empty state shows: "No decks yet. Create your first deck." with a New Deck button.

    8. Visit http://localhost:3000/songs — if no songs, confirm: "No songs yet. Search for a song to get started." with a search bar.
  </how-to-verify>
  <verify>Manual — see how-to-verify above</verify>
  <done>Human confirms job executed in jobs process, nav matches spec, empty states match spec text.</done>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
Full test suite:
```
cd /Users/hschin/Dev/praise-project && rails test
```

Must exit 0 with all tests green before the human checkpoint.

Phase 1 success criteria checklist:
- [ ] User can create an account with email and password and log in (AUTH-01)
- [ ] User session persists across browser refresh without re-login (AUTH-02)
- [ ] User can reset a forgotten password via email link (AUTH-03)
- [ ] Solid Queue background worker confirmed running via job execution (not just boot)
</verification>

<success_criteria>
- `rails test` exits 0 (full suite green)
- Procfile.dev has three processes: web, css, jobs
- development.rb sets queue_adapter to :solid_queue
- SmokeTestJob.perform_later executes and logs within seconds (human confirmed)
- Empty state text on /decks and /songs matches locked spec exactly
- Human checkpoint approved
</success_criteria>

<output>
After completion, create `.planning/phases/01-auth-foundation/01-03-SUMMARY.md` following the summary template.
</output>

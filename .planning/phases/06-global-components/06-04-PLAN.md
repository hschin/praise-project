---
phase: 06-global-components
plan: "04"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/views/devise/passwords/edit.html.erb
  - app/views/devise/passwords/new.html.erb
  - app/views/devise/unlocks/new.html.erb
  - app/views/devise/confirmations/new.html.erb
  - app/views/devise/shared/_error_messages.html.erb
autonomous: true
requirements:
  - FORM-01
  - FORM-02
  - FORM-03
gap_closure: true

must_haves:
  truths:
    - "All four remaining Devise views (passwords/edit, passwords/new, unlocks/new, confirmations/new) render with warm stone/rose palette — no bare scaffold HTML (div.field, div.actions, bare h2) remains"
    - "app/views/devise/shared/_error_messages.html.erb is deleted — zero references to devise/shared/error_messages exist anywhere in app/views/"
    - "passwords/new.html.erb displays inline styled error block (red-50 bg, red-700 text) matching registration form pattern — not the bare Devise h2+ul partial"
  artifacts:
    - path: "app/views/devise/passwords/edit.html.erb"
      provides: "Fully restyled change-password form — warm palette inputs, rose-700 submit"
    - path: "app/views/devise/passwords/new.html.erb"
      provides: "Inline error block replacing render 'devise/shared/error_messages'"
    - path: "app/views/devise/unlocks/new.html.erb"
      provides: "Fully restyled unlock instructions form — warm palette inputs, rose-700 submit"
    - path: "app/views/devise/confirmations/new.html.erb"
      provides: "Fully restyled resend confirmation form — warm palette inputs, rose-700 submit"
    - path: "app/views/devise/shared/_error_messages.html.erb"
      provides: "DELETED — no longer referenced by any view"
  key_links:
    - from: "app/views/devise/passwords/new.html.erb"
      to: "resource.errors.full_messages"
      via: "inline error block with resource.errors.any? guard"
      pattern: "resource\\.errors\\.any\\?"
    - from: "app/views/devise/passwords/edit.html.erb"
      to: "resource.errors.full_messages"
      via: "inline error block with resource.errors.any? guard"
      pattern: "resource\\.errors\\.any\\?"
---

<objective>
Restyle the three remaining bare-scaffold Devise views (passwords/edit, unlocks/new, confirmations/new) to use the warm stone/rose palette, replace the _error_messages partial render in passwords/new with an inline error block, then delete the now-unreferenced _error_messages.html.erb partial.

Purpose: Gap closure for FORM-01 — "all inputs, labels, buttons, and focus states across ALL pages use consistent warm palette styles." The verification report identified these four views as the only remaining FORM-01 blockers. Fixing them also unblocks deletion of the _error_messages partial (Gap 2 from VERIFICATION.md).
Output: Four Devise views fully styled matching registrations/new.html.erb pattern; _error_messages partial deleted; grep returns zero references.
</objective>

<execution_context>
@/Users/hschin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/hschin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-global-components/06-CONTEXT.md
@.planning/phases/06-global-components/06-RESEARCH.md

<interfaces>
<!-- Reference pattern: registrations/new.html.erb — the gold standard already styled correctly -->

From app/views/devise/registrations/new.html.erb (REFERENCE — match this pattern exactly):
```erb
<div class="max-w-md mx-auto mt-12">
  <h1 class="text-2xl font-bold text-center mb-2">Praise Project</h1>
  <h2 class="text-lg text-stone-600 text-center mb-6">[Page subtitle]</h2>

  <%= form_for(resource, as: resource_name, url: [action_path], html: { method: :post, class: "space-y-4" }) do |f| %>
    <%# Inline error block — NOT render "devise/shared/error_messages" %>
    <% if resource.errors.any? %>
      <div class="bg-red-50 border border-red-200 text-red-700 text-sm rounded-lg p-3">
        <% resource.errors.full_messages.each do |msg| %>
          <p><%= msg %></p>
        <% end %>
      </div>
    <% end %>

    <div>
      <%= f.label :email, class: "block text-sm font-medium text-stone-700 mb-1" %>
      <%= f.email_field :email, autofocus: true, autocomplete: "email",
          class: "w-full border border-stone-200 rounded-lg px-3 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-rose-600" %>
    </div>

    <%= f.submit "[Button label]",
        class: "w-full bg-rose-700 text-white text-sm px-4 py-2 rounded-lg hover:bg-rose-800 cursor-pointer" %>
  <% end %>

  <div class="mt-4 space-y-1 text-sm text-stone-500">
    <%= render "devise/shared/links" %>
  </div>
</div>
```

From app/views/devise/passwords/new.html.erb (CURRENT — already warm palette inputs, only needs inline error block):
```erb
<div class="max-w-md mx-auto mt-12">
  <h1 class="text-2xl font-bold text-center mb-2">Praise Project</h1>
  <h2 class="text-lg text-stone-600 text-center mb-6">Reset your password</h2>

  <%= form_for(resource, ...) do |f| %>
    <%= render "devise/shared/error_messages", resource: resource %>   <%# REPLACE with inline block %>
    <div>
      <%= f.label :email, class: "block text-sm font-medium text-stone-700 mb-1" %>
      <%= f.email_field :email, ..., class: "w-full border border-stone-200 rounded-lg px-3 py-2 text-sm ..." %>
    </div>
    <%= f.submit "Send reset instructions", class: "w-full bg-rose-700 ..." %>
  <% end %>
  ...
</div>
```

From app/views/devise/passwords/edit.html.erb (CURRENT — bare scaffold, needs full restyle):
```erb
<h2>Change your password</h2>
<%= form_for(resource, ..., html: { method: :put }) do |f| %>
  <%= render "devise/shared/error_messages", resource: resource %>
  <%= f.hidden_field :reset_password_token %>
  <div class="field">
    <p><%= f.label :password, "New password" %></p>
    <% if @minimum_password_length %>
      <p><em>(<%= @minimum_password_length %> characters minimum)</em></p>
    <% end %>
    <p><%= f.password_field :password, autofocus: true, autocomplete: "new-password" %></p>
  </div>
  <div class="field">
    <p><%= f.label :password_confirmation, "Confirm new password" %></p>
    <p><%= f.password_field :password_confirmation, autocomplete: "new-password" %></p>
  </div>
  <div class="actions">
    <%= f.submit "Change my password" %>
  </div>
<% end %>
<%= render "devise/shared/links" %>
```

From app/views/devise/unlocks/new.html.erb (CURRENT — bare scaffold):
```erb
<h2>Resend unlock instructions</h2>
<%= form_for(resource, ..., html: { method: :post }) do |f| %>
  <%= render "devise/shared/error_messages", resource: resource %>
  <div class="field">
    <p><%= f.label :email %></p>
    <p><%= f.email_field :email, autofocus: true, autocomplete: "email" %></p>
  </div>
  <div class="actions">
    <%= f.submit "Resend unlock instructions" %>
  </div>
<% end %>
<%= render "devise/shared/links" %>
```

From app/views/devise/confirmations/new.html.erb (CURRENT — bare scaffold):
```erb
<h2>Resend confirmation instructions</h2>
<%= form_for(resource, ..., html: { method: :post }) do |f| %>
  <%= render "devise/shared/error_messages", resource: resource %>
  <div class="field">
    <p><%= f.label :email %></p>
    <p><%= f.email_field :email, autofocus: true, autocomplete: "email", value: (...) %></p>
  </div>
  <div class="actions">
    <%= f.submit "Resend confirmation instructions" %>
  </div>
<% end %>
<%= render "devise/shared/links" %>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle three bare Devise views and fix passwords/new inline error block</name>
  <files>app/views/devise/passwords/edit.html.erb, app/views/devise/unlocks/new.html.erb, app/views/devise/confirmations/new.html.erb, app/views/devise/passwords/new.html.erb</files>
  <action>
    Rewrite all four files. Follow the registrations/new.html.erb pattern exactly (max-w-md container, Praise Project h1, warm h2 subtitle, space-y-4 form, inline error block, stone-200 borders, rose-600 focus rings, rose-700 submit, shared/links div).

    **passwords/new.html.erb** — already warm palette; only replace the `render "devise/shared/error_messages"` line with the inline error block. Do not change any other content.

    **passwords/edit.html.erb** — full rewrite:
    ```erb
    <div class="max-w-md mx-auto mt-12">
      <h1 class="text-2xl font-bold text-center mb-2">Praise Project</h1>
      <h2 class="text-lg text-stone-600 text-center mb-6">Change your password</h2>

      <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :put, class: "space-y-4" }) do |f| %>
        <% if resource.errors.any? %>
          <div class="bg-red-50 border border-red-200 text-red-700 text-sm rounded-lg p-3">
            <% resource.errors.full_messages.each do |msg| %>
              <p><%= msg %></p>
            <% end %>
          </div>
        <% end %>

        <%= f.hidden_field :reset_password_token %>

        <div>
          <%= f.label :password, "New password", class: "block text-sm font-medium text-stone-700 mb-1" %>
          <% if @minimum_password_length %>
            <p class="text-xs text-stone-400 mb-1"><%= @minimum_password_length %> characters minimum</p>
          <% end %>
          <%= f.password_field :password, autofocus: true, autocomplete: "new-password",
                class: "w-full border border-stone-200 rounded-lg px-3 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-rose-600" %>
        </div>

        <div>
          <%= f.label :password_confirmation, "Confirm new password", class: "block text-sm font-medium text-stone-700 mb-1" %>
          <%= f.password_field :password_confirmation, autocomplete: "new-password",
                class: "w-full border border-stone-200 rounded-lg px-3 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-rose-600" %>
        </div>

        <%= f.submit "Change my password",
            class: "w-full bg-rose-700 text-white text-sm px-4 py-2 rounded-lg hover:bg-rose-800 cursor-pointer" %>
      <% end %>

      <div class="mt-4 space-y-1 text-sm text-stone-500">
        <%= render "devise/shared/links" %>
      </div>
    </div>
    ```

    **unlocks/new.html.erb** — full rewrite:
    ```erb
    <div class="max-w-md mx-auto mt-12">
      <h1 class="text-2xl font-bold text-center mb-2">Praise Project</h1>
      <h2 class="text-lg text-stone-600 text-center mb-6">Resend unlock instructions</h2>

      <%= form_for(resource, as: resource_name, url: unlock_path(resource_name), html: { method: :post, class: "space-y-4" }) do |f| %>
        <% if resource.errors.any? %>
          <div class="bg-red-50 border border-red-200 text-red-700 text-sm rounded-lg p-3">
            <% resource.errors.full_messages.each do |msg| %>
              <p><%= msg %></p>
            <% end %>
          </div>
        <% end %>

        <div>
          <%= f.label :email, class: "block text-sm font-medium text-stone-700 mb-1" %>
          <%= f.email_field :email, autofocus: true, autocomplete: "email",
              class: "w-full border border-stone-200 rounded-lg px-3 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-rose-600" %>
        </div>

        <%= f.submit "Resend unlock instructions",
            class: "w-full bg-rose-700 text-white text-sm px-4 py-2 rounded-lg hover:bg-rose-800 cursor-pointer" %>
      <% end %>

      <div class="mt-4 space-y-1 text-sm text-stone-500">
        <%= render "devise/shared/links" %>
      </div>
    </div>
    ```

    **confirmations/new.html.erb** — full rewrite (preserve the `value:` conditional for pending reconfirmation):
    ```erb
    <div class="max-w-md mx-auto mt-12">
      <h1 class="text-2xl font-bold text-center mb-2">Praise Project</h1>
      <h2 class="text-lg text-stone-600 text-center mb-6">Resend confirmation instructions</h2>

      <%= form_for(resource, as: resource_name, url: confirmation_path(resource_name), html: { method: :post, class: "space-y-4" }) do |f| %>
        <% if resource.errors.any? %>
          <div class="bg-red-50 border border-red-200 text-red-700 text-sm rounded-lg p-3">
            <% resource.errors.full_messages.each do |msg| %>
              <p><%= msg %></p>
            <% end %>
          </div>
        <% end %>

        <div>
          <%= f.label :email, class: "block text-sm font-medium text-stone-700 mb-1" %>
          <%= f.email_field :email, autofocus: true, autocomplete: "email",
              value: (resource.pending_reconfirmation? ? resource.unconfirmed_email : resource.email),
              class: "w-full border border-stone-200 rounded-lg px-3 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-rose-600" %>
        </div>

        <%= f.submit "Resend confirmation instructions",
            class: "w-full bg-rose-700 text-white text-sm px-4 py-2 rounded-lg hover:bg-rose-800 cursor-pointer" %>
      <% end %>

      <div class="mt-4 space-y-1 text-sm text-stone-500">
        <%= render "devise/shared/links" %>
      </div>
    </div>
    ```

    After all four rewrites, verify no remaining render calls to the partial:
    `grep -r "error_messages" app/views/` — must return zero results before proceeding to Task 2.
  </action>
  <verify>
    <automated>rails test test/controllers/</automated>
  </verify>
  <done>All four views rewritten. No div.field, div.actions, or bare h2 scaffold HTML remains in any Devise view. All inputs have focus:ring-rose-600. grep returns zero results for "error_messages" in app/views/. rails test test/controllers/ is green.</done>
</task>

<task type="auto">
  <name>Task 2: Delete _error_messages.html.erb partial</name>
  <files>app/views/devise/shared/_error_messages.html.erb</files>
  <action>
    Now that all referencing views have been updated (Task 1), delete the partial:

    Run: `rm app/views/devise/shared/_error_messages.html.erb`

    Then verify deletion was successful and no broken references remain:
    - `ls app/views/devise/shared/` — _error_messages.html.erb should not appear
    - `grep -r "error_messages" app/views/` — must return zero results
    - `rails test` — full suite must pass (no ActionView::MissingTemplate errors)

    This task MUST only run after Task 1 is complete and grep confirms zero references.
  </action>
  <verify>
    <automated>rails test</automated>
  </verify>
  <done>_error_messages.html.erb is deleted. `grep -r "error_messages" app/views/` returns empty. Full test suite passes — no ActionView::MissingTemplate errors.</done>
</task>

</tasks>

<verification>
`grep -r "error_messages" app/views/` returns zero results
`ls app/views/devise/shared/` does not include _error_messages.html.erb
`rails test` full suite green
</verification>

<success_criteria>
- passwords/edit.html.erb, unlocks/new.html.erb, and confirmations/new.html.erb are fully restyled — no bare scaffold HTML, warm stone/rose palette throughout
- passwords/new.html.erb uses inline error block instead of render to _error_messages
- _error_messages.html.erb is deleted
- grep -r "error_messages" app/views/ returns empty
- FORM-01 gap closed: every Devise view uses consistent warm palette
- rails test passes (all controller tests green, no MissingTemplate errors)
</success_criteria>

<output>
After completion, create `.planning/phases/06-global-components/06-04-SUMMARY.md`
</output>

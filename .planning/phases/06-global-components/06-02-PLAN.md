---
phase: 06-global-components
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - test/controllers/registrations_controller_test.rb
  - app/views/devise/registrations/edit.html.erb
  - app/views/devise/registrations/new.html.erb
  - app/views/devise/shared/_error_messages.html.erb
autonomous: true
requirements:
  - FORM-01

must_haves:
  truths:
    - "The account edit page (GET /users/edit) renders with warm palette inputs — stone borders, rose-600 focus rings, rose-700 submit button — identical to the registration new page"
    - "Registration and account edit forms display inline styled error blocks (red-50 bg, red-700 text) when validation fails — not the bare Devise h2+ul scaffold"
    - "No reference to devise/shared/_error_messages exists in any view — the partial is deleted and both consuming views are updated"
  artifacts:
    - path: "app/views/devise/registrations/edit.html.erb"
      provides: "Fully restyled account edit form matching new.html.erb pattern"
    - path: "app/views/devise/registrations/new.html.erb"
      provides: "Inline error block replacing render 'devise/shared/error_messages'"
    - path: "app/views/devise/shared/_error_messages.html.erb"
      provides: "DELETED — no longer referenced"
  key_links:
    - from: "app/views/devise/registrations/new.html.erb"
      to: "resource.errors.full_messages"
      via: "inline error block with resource.errors.any? guard"
      pattern: "resource\\.errors\\.any\\?"
    - from: "app/views/devise/registrations/edit.html.erb"
      to: "resource.errors.full_messages"
      via: "inline error block with resource.errors.any? guard"
      pattern: "resource\\.errors\\.any\\?"
---

<objective>
Restyle the Devise account edit page (bare scaffold) to match the warm palette established in Phase 5, and replace the bare `_error_messages` partial in both registration views with the inline error block pattern already used by deck and song forms.

Purpose: FORM-01 — All inputs, labels, buttons, and focus states across ALL pages use consistent warm palette styles. The account edit page is the only remaining unstyled form.
Output: edit.html.erb fully restyled, new.html.erb updated with inline errors, _error_messages.html.erb deleted.
</objective>

<execution_context>
@/Users/hschin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/hschin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-global-components/06-CONTEXT.md
@.planning/phases/06-global-components/06-RESEARCH.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase. -->

From app/views/devise/registrations/new.html.erb (REFERENCE — already styled correctly):
```erb
<div class="max-w-md mx-auto mt-12">
  <h1 class="text-2xl font-bold text-center mb-2">Praise Project</h1>
  <h2 class="text-lg text-stone-600 text-center mb-6">Create account</h2>

  <%= form_for(resource, ...) do |f| %>
    <%= render "devise/shared/error_messages", resource: resource %>  <%# REPLACE with inline block %>

    <div>
      <%= f.label :email, class: "block text-sm font-medium text-stone-700 mb-1" %>
      <%= f.email_field :email, ..., class: "w-full border border-stone-200 rounded-lg px-3 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-rose-600" %>
    </div>
    ...
    <%= f.submit "Create account", class: "w-full bg-rose-700 text-white text-sm px-4 py-2 rounded-lg hover:bg-rose-800 cursor-pointer" %>
  <% end %>
</div>
```

From app/views/devise/registrations/edit.html.erb (CURRENT — bare scaffold, needs full restyle):
```erb
<h2>Edit User</h2>
<%= form_for(...) do |f| %>
  <%= render "devise/shared/error_messages", resource: resource %>
  <div class="field">
    <p><%= f.label :email %></p>
    <p><%= f.email_field :email, autofocus: true, autocomplete: "email" %></p>
  </div>
  ...
  <div class="actions">
    <%= f.submit "Update" %>
  </div>
<% end %>
<h3>Cancel my account</h3>
<div>Unhappy? <%= button_to "Cancel my account", registration_path(resource_name), data: { confirm: "Are you sure?", turbo_confirm: "Are you sure?" }, method: :delete %></div>
<%= link_to "Back", :back %>
```

Inline error block pattern (from decks/_form.html.erb and songs/_form.html.erb):
```erb
<% if resource.errors.any? %>
  <div class="bg-red-50 border border-red-200 text-red-700 text-sm rounded-lg p-3">
    <% resource.errors.full_messages.each do |msg| %>
      <p><%= msg %></p>
    <% end %>
  </div>
<% end %>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Devise edit page test case to registrations_controller_test.rb</name>
  <files>test/controllers/registrations_controller_test.rb</files>
  <behavior>
    - Test: GET /users/edit returns 200 and response body includes "focus:ring-rose-600" (warm palette focus ring class present on inputs)
    - Test verifies the edit page is styled — not bare scaffold HTML
  </behavior>
  <action>
    Add one test case to the existing test/controllers/registrations_controller_test.rb. The test signs in a user (using whatever auth setup pattern the file already uses), does GET to the edit_user_registration_path, asserts response is 200, and assert_match on "focus:ring-rose-600" or "border-stone-200" in response.body to confirm the warm palette is present.

    Do not create a new file — add within the existing test class.
  </action>
  <verify>
    <automated>MISSING — run after Task 2 creates the restyled view that this test verifies</automated>
  </verify>
  <done>One test case added to registrations_controller_test.rb verifying warm palette classes appear on the edit page</done>
</task>

<task type="auto">
  <name>Task 2: Restyle devise/registrations/edit.html.erb to match new.html.erb pattern</name>
  <files>app/views/devise/registrations/edit.html.erb</files>
  <action>
    Completely rewrite app/views/devise/registrations/edit.html.erb. Remove all bare scaffold HTML (`<div class="field">`, `<p>` wrappers, `<div class="actions">`, `<em>` tags). Replace with warm palette pattern matching new.html.erb.

    Structure:
    ```erb
    <div class="max-w-md mx-auto mt-12">
      <h1 class="text-2xl font-bold text-center mb-2">Praise Project</h1>
      <h2 class="text-lg text-stone-600 text-center mb-6">Account Settings</h2>

      <%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put, class: "space-y-4" }) do |f| %>
        <%# Inline error block — replaces render "devise/shared/error_messages" %>
        <% if resource.errors.any? %>
          <div class="bg-red-50 border border-red-200 text-red-700 text-sm rounded-lg p-3">
            <% resource.errors.full_messages.each do |msg| %>
              <p><%= msg %></p>
            <% end %>
          </div>
        <% end %>

        <div>
          <%= f.label :email, class: "block text-sm font-medium text-stone-700 mb-1" %>
          <%= f.email_field :email, autofocus: true, autocomplete: "email",
                class: "w-full border border-stone-200 rounded-lg px-3 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-rose-600" %>
        </div>

        <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
          <p class="text-sm text-stone-500">Currently waiting confirmation for: <%= resource.unconfirmed_email %></p>
        <% end %>

        <div>
          <%= f.label :password, class: "block text-sm font-medium text-stone-700 mb-1" %>
          <p class="text-xs text-stone-400 mb-1">Leave blank if you don't want to change it</p>
          <% if @minimum_password_length %>
            <p class="text-xs text-stone-400 mb-1"><%= @minimum_password_length %> characters minimum</p>
          <% end %>
          <%= f.password_field :password, autocomplete: "new-password",
                class: "w-full border border-stone-200 rounded-lg px-3 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-rose-600" %>
        </div>

        <div>
          <%= f.label :password_confirmation, class: "block text-sm font-medium text-stone-700 mb-1" %>
          <%= f.password_field :password_confirmation, autocomplete: "new-password",
                class: "w-full border border-stone-200 rounded-lg px-3 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-rose-600" %>
        </div>

        <div>
          <%= f.label :current_password, class: "block text-sm font-medium text-stone-700 mb-1" %>
          <p class="text-xs text-stone-400 mb-1">Required to confirm your changes</p>
          <%= f.password_field :current_password, autocomplete: "current-password",
                class: "w-full border border-stone-200 rounded-lg px-3 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-rose-600" %>
        </div>

        <%= f.submit "Update Account", class: "w-full bg-rose-700 text-white text-sm px-4 py-2 rounded-lg hover:bg-rose-800 cursor-pointer" %>
      <% end %>

      <div class="mt-6 pt-4 border-t border-stone-200">
        <%= button_to "Cancel my account", registration_path(resource_name),
              data: { turbo_confirm: "Are you sure? This cannot be undone." }, method: :delete,
              class: "text-sm text-stone-500 hover:text-red-600 bg-transparent border-none cursor-pointer" %>
      </div>
    </div>
    ```

    Notes:
    - De-emphasize cancel account button per research recommendation (`text-stone-500 hover:text-red-600`) — destructive but available
    - Keep `turbo_confirm:` on the delete button (DOM contract: do not remove data-turbo attributes)
    - Remove the `link_to "Back", :back` — no equivalent back link in new.html.erb; unnecessary in a settings page
    - Do NOT reference `devise/shared/error_messages` — use the inline block instead
  </action>
  <verify>
    <automated>rails test test/controllers/registrations_controller_test.rb</automated>
  </verify>
  <done>edit.html.erb contains no bare scaffold HTML (no div.field, no div.actions, no em/i tags). All inputs have focus:ring-rose-600. Test passes.</done>
</task>

<task type="auto">
  <name>Task 3: Replace _error_messages render in new.html.erb and delete partial</name>
  <files>app/views/devise/registrations/new.html.erb, app/views/devise/shared/_error_messages.html.erb</files>
  <action>
    Two changes, must happen atomically to avoid ActionView::MissingTemplate error:

    1. In app/views/devise/registrations/new.html.erb, replace line 6:
       ```erb
       <%= render "devise/shared/error_messages", resource: resource %>
       ```
       with the inline error block:
       ```erb
       <% if resource.errors.any? %>
         <div class="bg-red-50 border border-red-200 text-red-700 text-sm rounded-lg p-3">
           <% resource.errors.full_messages.each do |msg| %>
             <p><%= msg %></p>
           <% end %>
         </div>
       <% end %>
       ```

    2. Delete app/views/devise/shared/_error_messages.html.erb entirely. Use the Bash tool to run:
       `rm /path/to/app/views/devise/shared/_error_messages.html.erb`

    Verify no other files reference "devise/shared/error_messages" before deleting:
    `grep -r "error_messages" app/views/` — should return zero results after the deletion.

    Do NOT change any other content in new.html.erb — only replace the one render call.
  </action>
  <verify>
    <automated>rails test test/controllers/registrations_controller_test.rb</automated>
  </verify>
  <done>new.html.erb has inline error block instead of render call. _error_messages.html.erb file is deleted. `grep -r "error_messages" app/views/` returns empty. Tests pass.</done>
</task>

</tasks>

<verification>
`rails test test/controllers/registrations_controller_test.rb` green
`rails test test/controllers/` green
`grep -r "error_messages" app/views/` returns empty (partial fully removed and dereferenced)
</verification>

<success_criteria>
- edit.html.erb is fully restyled — no bare scaffold HTML, all inputs use warm palette
- new.html.erb uses inline error block — no render to _error_messages
- _error_messages.html.erb is deleted
- All registrations controller tests pass
- `rails test test/controllers/` green
</success_criteria>

<output>
After completion, create `.planning/phases/06-global-components/06-02-SUMMARY.md`
</output>

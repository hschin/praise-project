---
phase: 07-content-pages
plan: 03
type: execute
wave: 2
depends_on:
  - "07-01"
files_modified:
  - app/views/decks/show.html.erb
  - app/views/songs/index.html.erb
autonomous: true
requirements:
  - EMPTY-02
  - EMPTY-03
must_haves:
  truths:
    - "A user opening a deck with no songs sees a small icon and the copy 'Your arrangement will appear here. Add a song from the left panel.'"
    - "A user opening the song library with no songs sees the copy 'Import a song above to build your library.'"
    - "The deck editor empty state is inside the sortable div's else branch — the drag-and-drop contract for the if branch is not touched"
    - "The song library empty state replaces only the else branch text — the form and filter above remain unchanged"
  artifacts:
    - path: "app/views/decks/show.html.erb"
      provides: "EMPTY-02 — icon + instruction inside arrangement panel else branch"
      contains: "Your arrangement will appear here"
    - path: "app/views/songs/index.html.erb"
      provides: "EMPTY-03 — icon + single-line copy in songs else branch"
      contains: "Import a song above to build your library"
  key_links:
    - from: "app/views/decks/show.html.erb"
      to: "sortable div else branch (line ~93)"
      via: "replacing plain <p> with icon + copy div"
      pattern: "Your arrangement will appear here"
    - from: "app/views/songs/index.html.erb"
      to: "songs else branch (line ~59)"
      via: "replacing two-line text block with icon + copy div"
      pattern: "Import a song above"
---

<objective>
Replace the plain-text empty states in the deck editor arrangement panel and the song library with small icon + copy empty states.

Purpose: EMPTY-02 — deck editors know exactly where to look and what to do when a deck has no songs. EMPTY-03 — new users understand the import workflow without extra orientation.
Output: Targeted edits to two else branches in existing view files. No structural changes to the if branches.
</objective>

<execution_context>
@/Users/hschin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/hschin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-content-pages/07-CONTEXT.md
@.planning/phases/07-content-pages/07-RESEARCH.md
@.planning/phases/07-content-pages/07-01-SUMMARY.md

<interfaces>
<!-- Target locations — edit only the else branches -->

From app/views/decks/show.html.erb (~lines 85-95):
```erb
<div data-controller="sortable"
     data-sortable-url-value="<%= reorder_deck_deck_song_path(@deck, ':id') %>"
     class="space-y-3">
  <% if @deck.deck_songs.any? %>
    <% @deck.deck_songs.each do |deck_song| %>
      <%= render "deck_songs/song_block", deck: @deck, deck_song: deck_song %>
    <% end %>
  <% else %>
    <p class="text-stone-400 text-sm">No songs added yet.</p>   ← REPLACE THIS LINE ONLY
  <% end %>
</div>
```

CRITICAL: Do NOT modify the `if` branch, the sortable controller data attributes, or any wrapper divs. Only replace the `<p>` on the else branch.

From app/views/songs/index.html.erb (~lines 59-64):
```erb
<% else %>
  <div class="text-center py-12 text-stone-400">
    <p class="text-lg mb-1">No songs yet</p>              ← REPLACE THIS BLOCK
    <p class="text-sm">Search for a song above to get started</p>
  </div>
<% end %>
```

Replace the entire `<div class="text-center py-12 text-stone-400">` block with the new empty state. Keep the surrounding `<% else %>` and `<% end %>` tags.

Established icon pattern (Heroicons inline SVG, no npm):
- For deck editor: use musical-note or queue-list icon (small, w-8 h-8)
- For song library: use musical-note icon (small, w-8 h-8)
- Color: text-stone-300 on the icon div
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace empty states in deck show and song library views</name>
  <files>app/views/decks/show.html.erb, app/views/songs/index.html.erb</files>
  <behavior>
    - Test "deck show page renders empty state copy when deck has no songs" passes: response.body includes "Your arrangement will appear here"
    - Test "GET /songs shows empty state copy when no songs exist" passes: response.body includes "Import a song above to build your library"
  </behavior>
  <action>
    **In app/views/decks/show.html.erb:**

    Find the else branch inside the sortable div (currently line ~93):
    ```erb
    <p class="text-stone-400 text-sm">No songs added yet.</p>
    ```

    Replace ONLY that line with:
    ```erb
    <div class="text-center py-8">
      <div class="mx-auto w-8 h-8 text-stone-300 mb-2">
        <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-8 h-8">
          <path stroke-linecap="round" stroke-linejoin="round" d="m9 9 10.5-3m0 6.553v3.75a2.25 2.25 0 0 1-1.632 2.163l-1.32.377a1.803 1.803 0 1 1-.99-3.467l2.31-.66a2.25 2.25 0 0 0 1.632-2.163Zm0 0V2.25L9 5.25v10.303m0 0v3.75a2.25 2.25 0 0 1-1.632 2.163l-1.32.377a1.803 1.803 0 0 1-.99-3.467l2.31-.66A2.25 2.25 0 0 0 9 15.553Z" />
        </svg>
      </div>
      <p class="text-sm text-stone-400">Your arrangement will appear here. Add a song from the left panel.</p>
    </div>
    ```

    Use the Heroicons `musical-note` SVG path (shown above) as the icon.

    **In app/views/songs/index.html.erb:**

    Find the else branch (~lines 59-64):
    ```erb
    <div class="text-center py-12 text-stone-400">
      <p class="text-lg mb-1">No songs yet</p>
      <p class="text-sm">Search for a song above to get started</p>
    </div>
    ```

    Replace that entire div with:
    ```erb
    <div class="text-center py-8">
      <div class="mx-auto w-8 h-8 text-stone-300 mb-2">
        <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-8 h-8">
          <path stroke-linecap="round" stroke-linejoin="round" d="m9 9 10.5-3m0 6.553v3.75a2.25 2.25 0 0 1-1.632 2.163l-1.32.377a1.803 1.803 0 1 1-.99-3.467l2.31-.66a2.25 2.25 0 0 0 1.632-2.163Zm0 0V2.25L9 5.25v10.303m0 0v3.75a2.25 2.25 0 0 1-1.632 2.163l-1.32.377a1.803 1.803 0 0 1-.99-3.467l2.31-.66A2.25 2.25 0 0 0 9 15.553Z" />
        </svg>
      </div>
      <p class="text-sm text-stone-400">Import a song above to build your library.</p>
    </div>
    ```
  </action>
  <verify>
    <automated>cd /Users/hschin/Dev/praise-project && rails test test/controllers/decks_controller_test.rb test/controllers/songs_controller_test.rb 2>&1 | tail -20</automated>
  </verify>
  <done>
    - EMPTY-02 test passes GREEN: "deck show page renders empty state copy when deck has no songs"
    - EMPTY-03 test passes GREEN: "GET /songs shows empty state copy when no songs exist"
    - All pre-existing tests remain green
    - Sortable div if-branch and data attributes untouched (drag-and-drop contract preserved)
  </done>
</task>

</tasks>

<verification>
```bash
cd /Users/hschin/Dev/praise-project && rails test test/controllers/decks_controller_test.rb test/controllers/songs_controller_test.rb
```

All tests pass. EMPTY-02 and EMPTY-03 assertions are GREEN.
</verification>

<success_criteria>
- show.html.erb else branch contains "Your arrangement will appear here. Add a song from the left panel." with small icon
- songs/index.html.erb else branch contains "Import a song above to build your library." with small icon
- Sortable div `data-controller="sortable"` and all data attributes unchanged
- Song library forms (import, filter) unchanged
- All targeted tests GREEN
</success_criteria>

<output>
After completion, create `.planning/phases/07-content-pages/07-03-SUMMARY.md`
</output>

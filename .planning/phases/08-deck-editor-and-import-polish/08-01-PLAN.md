---
phase: 08-deck-editor-and-import-polish
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - test/controllers/decks_controller_test.rb
  - test/controllers/songs_controller_test.rb
autonomous: true
requirements:
  - DECK-01
  - DECK-02
  - DECK-03
  - DECK-04
  - DECK-05
  - DECK-06
  - IMPORT-01
  - IMPORT-02

must_haves:
  truths:
    - "All 9 new test cases exist in the test files and run (red — they will fail before implementation)"
    - "Running rails test test/controllers/decks_controller_test.rb test/controllers/songs_controller_test.rb exits cleanly with failures (not errors)"
  artifacts:
    - path: "test/controllers/decks_controller_test.rb"
      provides: "7 new test methods for DECK-01 through DECK-06"
      contains: "test_deck_show_renders_section_chip_for_slide_items"
    - path: "test/controllers/songs_controller_test.rb"
      provides: "2 new test methods for IMPORT-01 and IMPORT-02"
      contains: "test_processing_page_renders_claude_copy"
  key_links:
    - from: "test/controllers/decks_controller_test.rb"
      to: "app/views/decks/show.html.erb"
      via: "get deck_url(decks(:one)) → assert_select"
      pattern: "assert_select"
    - from: "test/controllers/songs_controller_test.rb"
      to: "app/views/songs/_processing.html.erb"
      via: "get processing_songs_path → assert_select"
      pattern: "assert_select"
---

<objective>
Create failing test scaffolding for all 9 Phase 8 requirements. Tests must run cleanly (fail, not error) so Wave 1 plans have a RED baseline to implement against.

Purpose: Nyquist compliance — every production change in Wave 1 has a pre-existing automated test.
Output: 9 new test methods across 2 existing test files, all initially failing.
</objective>

<execution_context>
@/Users/hschin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/hschin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-deck-editor-and-import-polish/08-CONTEXT.md
@.planning/phases/08-deck-editor-and-import-polish/08-RESEARCH.md

<interfaces>
<!-- Existing test file patterns extracted from codebase. -->

From test/controllers/decks_controller_test.rb (existing pattern):
```ruby
test "deck show page renders slide preview divs for arrangement entries" do
  get deck_url(decks(:one))
  assert_select ".slide-preview"
end
```

From test/controllers/songs_controller_test.rb (existing pattern):
```ruby
test "GET show for failed song renders Couldn't find lyrics copy with title" do
  # uses songs(:one), sign_in @user, get song_url(...)
end
```

Fixtures available:
- decks(:one) — has deck_songs via deck_songs(:one) fixture
- songs(:one) — used for song controller tests
- users(:one) — sign_in @user in setup
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add deck editor test scaffolding (DECK-01 through DECK-06)</name>
  <files>test/controllers/decks_controller_test.rb</files>
  <behavior>
    - test_deck_show_renders_section_chip_for_slide_items: GET deck_url(decks(:one)), assert_select with CSS selector for chip class (e.g., "span.rounded-full") or assert_match for "bg-amber-100" in response body — fails until _slide_item.html.erb updated
    - test_deck_show_renders_artist_in_library_panel: GET deck_url(decks(:one)), check library list item includes song artist text below title (assert_select for data-song-search-target="item") — fails until show.html.erb updated
    - test_export_button_idle_renders_download_icon: GET deck_url(decks(:one)), assert_match /arrow-down-tray|M10\.75 2\.75/ in response body (SVG path fragment) — fails until _export_button.html.erb updated
    - test_export_button_ready_renders_green_button: render export_button partial with state: :ready, assert_select with "bg-green-600" class and check-circle SVG present — fails until _export_button.html.erb updated
    - test_deck_show_panel_labels: GET deck_url(decks(:one)), assert_select response body contains "ADD SONGS" and "ARRANGEMENT" as separate labels — fails until show.html.erb updated
    - test_deck_title_pencil_hover_only: GET deck_url(decks(:one)), assert_match /opacity-0.*group-hover:opacity-100|group-hover:opacity-100.*opacity-0/ in pencil button classes — fails until show.html.erb updated
    - test_deck_show_renders_auto_save_indicator: GET deck_url(decks(:one)), assert_select "[data-auto-save-target='indicator']" — fails until show.html.erb updated
  </behavior>
  <action>
    Append 7 new test methods to test/controllers/decks_controller_test.rb, immediately before the final `end` of the class.

    For test_export_button_ready_renders_green_button: Use a deck fixture and render the partial inline via `get deck_url(decks(:one))` — the export button is rendered in the deck show page (state: :idle by default). For the ready-state test, either call render directly in test or use assert_match on the partial's output. The simplest approach: create a test that renders _export_button.html.erb partial with state: :ready using ActionView::TestCase patterns, OR just note that the green-600 state is checked via a separate get request with a job-produced export. Given the complexity of job-triggered state, assert_match on GET deck_url for idle icon is sufficient; for ready state, use `render` inline in test or check by inspecting partial directly.

    Simplest viable approach: For ready state test, use `get deck_url(decks(:one))` (which renders idle state) and separately test the partial with `render partial: "decks/export_button", locals: {deck: decks(:one), state: :ready, token: "abc"}` in an ActionDispatch::IntegrationTest by calling the partials/renders directly. OR: assert_select on the full page response for idle state icon presence, and for ready state: add a fixture or test that exercises the export path.

    Pragmatic approach to avoid complexity: The idle state test checks for the SVG path string in the full deck show page response. The ready state test uses assert_match on rendered partial content via `render_to_string`. Check Rails ActionView::TestCase if available; otherwise use controller test with assert_match on response.body.

    Use assert_match for text/class presence in response.body where assert_select is insufficient for SVG content or multi-class strings.
  </action>
  <verify>
    <automated>rails test test/controllers/decks_controller_test.rb 2>&1 | tail -5</automated>
  </verify>
  <done>7 new test methods exist, test runner reports failures (not errors/exceptions) for all 7 new tests</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add import flow test scaffolding (IMPORT-01, IMPORT-02)</name>
  <files>test/controllers/songs_controller_test.rb</files>
  <behavior>
    - test_processing_page_renders_claude_copy: GET processing_songs_path(title: "讚美之泉"), assert_match /Claude is structuring your lyrics/ in response.body — fails until _processing.html.erb updated
    - test_song_show_done_renders_add_to_deck_link: GET song_url(songs(:one)) where songs(:one).done? returns true, assert_select "a[href*='decks']" with text matching "Add this song to a deck" — fails until songs/show.html.erb updated. If songs(:one) is not done? status, update fixture or create done song inline.
  </behavior>
  <action>
    Append 2 new test methods to test/controllers/songs_controller_test.rb before the final `end`.

    For test_processing_page_renders_claude_copy: `get processing_songs_path(title: "讚美之泉")` — the processing page is a separate route that renders `songs/processing.html.erb` using `songs/processing_controller.js` Turbo Stream. Check if the processing route renders without a song record, or create a pending song first.

    For test_song_show_done_renders_add_to_deck_link: Check songs(:one) fixture status. If import_status is not "done", create an inline done song: `song = Song.create!(title: "Test", import_status: "done")`. Then `get song_url(song)` and assert the "Add this song to a deck" link exists.

    Read the songs fixtures file first to verify songs(:one) import_status before deciding approach.
  </action>
  <verify>
    <automated>rails test test/controllers/songs_controller_test.rb 2>&1 | tail -5</automated>
  </verify>
  <done>2 new test methods exist, test runner reports failures (not errors) for both new tests; existing tests remain green</done>
</task>

</tasks>

<verification>
rails test test/controllers/decks_controller_test.rb test/controllers/songs_controller_test.rb

Expected: 9 new failures visible, 0 errors, existing tests still green.
</verification>

<success_criteria>
- All 9 new test methods exist in the two test files
- `rails test test/controllers/decks_controller_test.rb test/controllers/songs_controller_test.rb` produces failures (not exceptions/errors) for the new tests
- All pre-existing test cases still pass
</success_criteria>

<output>
After completion, create `.planning/phases/08-deck-editor-and-import-polish/08-01-SUMMARY.md`
</output>

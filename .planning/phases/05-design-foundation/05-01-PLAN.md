---
phase: 05-design-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/assets/tailwind/application.css
  - app/views/decks/_export_button.html.erb
  - app/views/decks/_form.html.erb
  - app/views/decks/index.html.erb
  - app/views/deck_songs/_song_block.html.erb
  - app/views/deck_songs/_slide_item.html.erb
  - app/views/songs/index.html.erb
  - app/views/songs/show.html.erb
  - app/views/songs/edit.html.erb
  - app/views/songs/_form.html.erb
  - app/views/songs/_lyrics.html.erb
  - app/views/songs/_failed.html.erb
  - app/views/songs/_processing.html.erb
  - app/views/songs/processing.html.erb
  - app/views/themes/_form.html.erb
  - app/views/themes/_applied_theme.html.erb
  - app/views/themes/_suggestion_card.html.erb
  - app/views/devise/sessions/new.html.erb
  - app/views/devise/registrations/new.html.erb
  - app/views/devise/passwords/new.html.erb
  - app/views/devise/shared/_links.html.erb
autonomous: true
requirements:
  - VIS-01

must_haves:
  truths:
    - "No indigo-* Tailwind class appears in any view file (excluding application.html.erb which is handled by Plan 02)"
    - "No gray-* Tailwind class appears in any view file (excluding application.html.erb)"
    - "Warm amber and stone tokens are defined in the @theme block in application.css"
  artifacts:
    - path: "app/assets/tailwind/application.css"
      provides: "@theme token block with worship-primary, worship-surface, worship-nav, worship-border, worship-text, worship-secondary, worship-muted"
      contains: "@theme"
  key_links:
    - from: "app/assets/tailwind/application.css"
      to: "app/views/**/*.html.erb"
      via: "Tailwind utility class generation from @theme tokens"
      pattern: "@theme"
---

<objective>
Define the Tailwind v4 design token foundation and replace all indigo/gray Tailwind classes with warm amber/stone equivalents across the 21 non-layout ERB view files.

Purpose: Establish the canonical color token source of truth and eliminate every legacy indigo and gray class from views so that the app displays the warm, worshipful palette (VIS-01).
Output: An @theme block in application.css; 21 view files with all indigo-* and gray-* classes replaced using the mapping table below.
</objective>

<execution_context>
@/Users/hschin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/hschin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-design-foundation/05-CONTEXT.md
@.planning/phases/05-design-foundation/05-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add @theme token block to application.css</name>
  <files>app/assets/tailwind/application.css</files>
  <action>
    Append an @theme block immediately after the `@import "tailwindcss";` line. Use CSS variable aliasing from Tailwind v4's built-in palette:

    ```css
    @import "tailwindcss";

    @theme {
      --color-worship-primary: var(--color-amber-800);
      --color-worship-primary-hover: var(--color-amber-900);
      --color-worship-surface: var(--color-stone-50);
      --color-worship-nav: var(--color-stone-100);
      --color-worship-border: var(--color-stone-200);
      --color-worship-text: var(--color-stone-900);
      --color-worship-secondary: var(--color-stone-600);
      --color-worship-muted: var(--color-stone-500);
    }
    ```

    IMPORTANT: If `var(--color-amber-800)` aliasing proves unreliable during execution (i.e., generated classes render unstyled), fall back to direct hex values matching Tailwind's built-in amber-800 (#92400e) and stone equivalents. Note this fallback in the task output.

    DO NOT modify `app/assets/stylesheets/application.css` — that file contains the `.pinyin-hidden` class which is a JavaScript contract.
  </action>
  <verify>
    <automated>grep -n "@theme" /Users/hschin/Dev/praise-project/app/assets/tailwind/application.css</automated>
  </verify>
  <done>@theme block exists in application.css with all 7 worship-* token definitions</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Palette replacement pass — 21 non-layout view files</name>
  <files>
    app/views/decks/_export_button.html.erb,
    app/views/decks/_form.html.erb,
    app/views/decks/index.html.erb,
    app/views/deck_songs/_song_block.html.erb,
    app/views/deck_songs/_slide_item.html.erb,
    app/views/songs/index.html.erb,
    app/views/songs/show.html.erb,
    app/views/songs/edit.html.erb,
    app/views/songs/_form.html.erb,
    app/views/songs/_lyrics.html.erb,
    app/views/songs/_failed.html.erb,
    app/views/songs/_processing.html.erb,
    app/views/songs/processing.html.erb,
    app/views/themes/_form.html.erb,
    app/views/themes/_applied_theme.html.erb,
    app/views/themes/_suggestion_card.html.erb,
    app/views/devise/sessions/new.html.erb,
    app/views/devise/registrations/new.html.erb,
    app/views/devise/passwords/new.html.erb,
    app/views/devise/shared/_links.html.erb,
    app/views/decks/show.html.erb
  </files>
  <action>
    Apply the following mechanical class substitutions to every file listed above. Work through each file one at a time — treat this list as a checklist.

    NOTE about decks/show.html.erb: This file is also modified by Plan 02 (Task 2 — nav restructure) and Plan 04 (Task 2 — inline-edit header). Apply ONLY the palette replacement substitutions here. Do not restructure the header or add new Stimulus controllers in this task.

    **Replacement table (apply all occurrences per file):**

    | Old class | New class |
    |-----------|-----------|
    | `bg-indigo-600` | `bg-amber-800` |
    | `hover:bg-indigo-700` | `hover:bg-amber-900` |
    | `text-indigo-600` | `text-amber-800` |
    | `hover:text-indigo-600` | `hover:text-amber-800` |
    | `focus:ring-indigo-500` | `focus:ring-amber-600` |
    | `bg-indigo-50` | `bg-amber-50` |
    | `hover:bg-indigo-50` | `hover:bg-amber-50` |
    | `border-indigo-300` | `border-amber-300` |
    | `text-indigo-400` | `text-amber-700` |
    | `text-indigo-500` | `text-amber-700` |
    | `border-indigo-100` | `border-amber-100` |
    | `bg-indigo-400` | `bg-amber-700` |
    | `bg-gray-50` | `bg-stone-50` |
    | `border-gray-200` | `border-stone-200` |
    | `border-gray-300` | `border-stone-200` |
    | `text-gray-900` | `text-stone-900` |
    | `text-gray-600` | `text-stone-600` |
    | `text-gray-500` | `text-stone-500` |
    | `text-gray-400` | `text-stone-400` |
    | `divide-gray-100` | `divide-stone-100` |
    | `border-gray-100` | `border-stone-100` |
    | `text-gray-800` | `text-stone-800` |
    | `text-gray-700` | `text-stone-700` |
    | `text-gray-300` | `text-stone-300` |
    | `bg-gray-700` | `bg-stone-700` |
    | `hover:bg-gray-800` | `hover:bg-stone-800` |
    | `bg-gray-100` | `bg-stone-100` |
    | `hover:bg-gray-100` | `hover:bg-stone-100` |
    | `bg-gray-200` | `bg-stone-200` |
    | `hover:text-gray-900` | `hover:text-stone-900` |
    | `hover:text-gray-600` | `hover:text-stone-600` |
    | `border-gray-400` | `border-stone-400` |

    DOM CONTRACT — DO NOT change:
    - `id="export_button_..."`, `id="theme_suggestions"`, `id="import_status"`, `id="song_status_..."` — never rename these IDs
    - `data-drag-handle` and `data-id` attributes — do not touch
    - `.pinyin-hidden` — not in these files, but confirm you do not touch application.css stylesheet
    - `data: { turbo: false }` on the import forms in decks/show — preserve exactly
    - `content_for(:main_class)` in decks/show — preserve exactly

    Typography classes (rounded/shadow component language, VIS-02) in these files:
    - Replace `rounded` (bare, no suffix) with `rounded-lg` when on buttons or inputs
    - Replace `shadow` (bare) with `shadow-sm` when on card containers
    - Keep `rounded-lg`, `rounded-xl`, `shadow-sm` as-is
    - Keep `rounded-full` (used for avatars/pills) as-is

    After completing all files, run the verification grep to confirm no indigo remains.
  </action>
  <verify>
    <automated>grep -r "indigo\|bg-gray-\|text-gray-\|border-gray-" /Users/hschin/Dev/praise-project/app/views/ --include="*.erb" -l | grep -v "application.html.erb"</automated>
  </verify>
  <done>grep returns no files (empty output). All 21 files have had indigo and gray classes replaced with amber/stone equivalents. The rails test suite passes: `rails test test/controllers/decks_controller_test.rb`</done>
</task>

</tasks>

<verification>
1. `grep -n "@theme" app/assets/tailwind/application.css` — shows @theme block
2. `grep -r "indigo" app/views/ --include="*.erb" -l | grep -v application.html.erb` — empty output
3. `grep -r "bg-gray-\|text-gray-\|border-gray-" app/views/ --include="*.erb" -l | grep -v application.html.erb` — empty output
4. `rails test test/controllers/decks_controller_test.rb` — all tests pass
</verification>

<success_criteria>
- @theme block defined in application.css with 7 worship-* tokens referencing Tailwind's built-in palette
- Zero indigo-* classes remaining in any of the 21 non-layout view files
- Zero gray-* classes remaining in any of the 21 non-layout view files
- Bare `rounded` on buttons/inputs replaced with `rounded-lg`; bare `shadow` on cards replaced with `shadow-sm`
- All Turbo Stream IDs, sortable data attributes, and form contracts preserved intact
- Test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/05-design-foundation/05-01-SUMMARY.md`
</output>

<div class="max-w-2xl mx-auto px-4 py-8">
  <div class="flex items-center justify-between mb-6">
    <h1 class="text-2xl font-bold text-stone-900">Edit Song</h1>
    <%= link_to "Cancel", song_path(@song), class: "text-sm text-stone-500 hover:underline" %>
  </div>

  <%= render "form", song: @song %>
</div>

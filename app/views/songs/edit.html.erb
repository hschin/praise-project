<div class="max-w-2xl mx-auto px-4 py-8">
  <div class="flex items-center justify-between mb-6">
    <h1 class="font-headline text-3xl text-on-surface">Edit Song</h1>
    <%= link_to "Cancel", song_path(@song), class: "text-sm text-on-surface-variant hover:text-primary transition-colors font-body" %>
  </div>

  <%= render "form", song: @song %>
</div>

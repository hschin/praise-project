<%= turbo_stream_from "song_import_#{@title.parameterize}" %>

<div class="max-w-2xl mx-auto px-4 py-8">
  <div class="flex items-center justify-between mb-6">
    <h1 class="text-2xl font-bold text-stone-900"><%= @title %></h1>
    <% if @deck_id.present? %>
      <%= link_to "Back to Deck", deck_path(@deck_id), class: "text-sm text-rose-700 hover:underline" %>
    <% else %>
      <%= link_to "Back to Library", songs_path, class: "text-sm text-rose-700 hover:underline" %>
    <% end %>
  </div>

  <div id="import_status">
    <%= render "songs/processing", step: "searching" %>
  </div>
</div>

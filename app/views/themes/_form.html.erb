<%# locals: (deck:) %>
<%= form_with url: deck_themes_path(deck), multipart: true, class: "space-y-4" do |f| %>
  <div class="flex gap-6">
    <div data-controller="color-picker">
      <%= label_tag :theme_background_color, "Background Color", class: "block text-sm font-medium text-stone-700 mb-1" %>
      <div class="flex items-center gap-2">
        <%= color_field_tag "theme[background_color]", "#000000",
            data: { "color-picker-target": "picker", action: "input->color-picker#pickerChanged" },
            class: "w-10 h-8 rounded-lg border border-stone-200 cursor-pointer p-0.5" %>
        <%= text_field_tag nil, "#000000",
            data: { "color-picker-target": "hex", action: "input->color-picker#hexChanged" },
            class: "w-24 border border-stone-200 rounded-lg px-2 py-1 text-xs font-mono",
            placeholder: "#000000" %>
      </div>
    </div>

    <div data-controller="color-picker">
      <%= label_tag :theme_text_color, "Text Color", class: "block text-sm font-medium text-stone-700 mb-1" %>
      <div class="flex items-center gap-2">
        <%= color_field_tag "theme[text_color]", "#ffffff",
            data: { "color-picker-target": "picker", action: "input->color-picker#pickerChanged" },
            class: "w-10 h-8 rounded-lg border border-stone-200 cursor-pointer p-0.5" %>
        <%= text_field_tag nil, "#ffffff",
            data: { "color-picker-target": "hex", action: "input->color-picker#hexChanged" },
            class: "w-24 border border-stone-200 rounded-lg px-2 py-1 text-xs font-mono",
            placeholder: "#ffffff" %>
      </div>
    </div>
  </div>

  <div>
    <%= label_tag :theme_font_size, "Font Size", class: "block text-sm font-medium text-stone-700 mb-1" %>
    <%= select_tag "theme[font_size]",
        options_for_select([["Small (28pt)", "small"], ["Medium (36pt)", "medium"], ["Large (48pt)", "large"]], "medium"),
        class: "border border-stone-200 rounded-lg px-3 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-rose-600" %>
  </div>

  <div>
    <%= label_tag :theme_background_image, "Background Image (optional)", class: "block text-sm font-medium text-stone-700 mb-1" %>
    <% if deck.theme&.background_image&.attached? %>
      <label class="flex items-center gap-2 mb-2 text-sm text-stone-600 cursor-pointer">
        <%= check_box_tag "theme[remove_background_image]", "1", false, class: "rounded-lg" %>
        Remove current background image
      </label>
    <% end %>
    <%= file_field_tag "theme[background_image]",
        accept: "image/*",
        class: "text-sm text-stone-500 file:mr-3 file:py-1 file:px-3 file:rounded-lg file:border-0 file:text-sm file:bg-rose-50 file:text-rose-700 hover:file:bg-rose-100" %>
    <p class="text-xs text-stone-400 mt-1">Replaces background color when provided. JPEG, PNG, WebP.</p>
  </div>

  <div>
    <%= submit_tag "Apply Theme",
        class: "bg-rose-700 text-white text-sm px-4 py-2 rounded-lg hover:bg-rose-800 cursor-pointer" %>
  </div>
<% end %>

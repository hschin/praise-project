Hello <%= @resource.email %>,

Someone requested a link to reset your Praise Project password.
Use the link below to choose a new one:

  <%= edit_password_url(@resource, reset_password_token: @token) %>

This link expires in <%= Devise.reset_password_within / 3600 %> hours.

If you didn't request a password reset, you can safely ignore this email —
your password won't change.

---
Praise Project · Built to project his praise.

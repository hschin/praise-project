<%= form_with model: deck, class: "space-y-4" do |f| %>
  <% if deck.errors.any? %>
    <div class="bg-red-50 border border-red-200 text-red-700 text-sm rounded-lg p-3">
      <% deck.errors.full_messages.each do |msg| %>
        <p><%= msg %></p>
      <% end %>
    </div>
  <% end %>

  <div>
    <%= f.label :title, "Title", class: "block text-sm font-medium text-stone-700 mb-1" %>
    <%= f.text_field :title, class: "w-full border border-stone-200 rounded-lg px-3 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-rose-600" %>
  </div>

  <div>
    <%= f.label :date, "Date", class: "block text-sm font-medium text-stone-700 mb-1" %>
    <%= f.date_field :date, class: "w-full border border-stone-200 rounded-lg px-3 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-rose-600" %>
  </div>

  <div>
    <%= f.label :notes, "Notes", class: "block text-sm font-medium text-stone-700 mb-1" %>
    <%= f.text_area :notes, rows: 3, class: "w-full border border-stone-200 rounded-lg px-3 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-rose-600" %>
  </div>

  <div class="flex gap-3 pt-2">
    <%= f.submit class: "bg-rose-700 text-white text-sm px-4 py-2 rounded-lg hover:bg-rose-800 cursor-pointer" %>
    <%= link_to "Cancel", decks_path, class: "text-sm text-stone-500 hover:underline self-center" %>
  </div>
<% end %>
